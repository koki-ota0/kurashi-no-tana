# 暮らしの棚 改善計画（v1）

## 1. 目的
- 毎日使いたくなる「記録しやすさ」と「振り返りやすさ」を高める。
- 運用に耐える最低限の品質（セキュリティ・テスト・保守性）を整える。

## 2. 現状の観点整理

### 2.1 ユーザー体験（UX）
- ホームはカテゴリ絞り込みと一覧表示が中心で、検索・並び替え・フィルタ条件保存がない。
- 画像アップロードや入力補助（ブランド候補、カテゴリ管理など）が弱く、登録体験を短縮しづらい。
- 発見ページは「新着・カテゴリ人気」までで、レコメンドの深さが浅い。

### 2.2 技術課題
- `DEFAULT_USER_ID = 1L` 前提の実装で実ユーザー運用に展開しにくい。
- `application.yml` に DB 認証情報が直書きされており、秘匿情報管理の改善余地が大きい。
- 依存関係に重複（`spring-boot-starter-webmvc` と `spring-boot-starter-web` 併用）があり、構成が不明瞭。
- テストがほぼ初期状態で、回帰を防ぐ仕組みが不足。

## 3. 改善ロードマップ（優先順）

### Phase 0（今週）: 安全性・基盤の最低ライン
1. **設定の安全化**
   - DB 接続情報を環境変数化し、`application.yml` はプレースホルダ化する。
   - `.env.example` を追加してセットアップ手順を明文化。
2. **依存整理**
   - Web 系 starter の重複を解消し、必要依存のみ残す。
3. **テスト土台**
   - `ItemService` の主要ユースケース（作成・更新・使用回数加算）のユニットテストを追加。
   - `WebController` の主要ページ疎通テストを追加。

**完了条件**
- 秘匿情報がリポジトリに含まれない。
- CI 相当コマンド（`./mvnw test`）が安定して通る。

### Phase 1（1〜2 週間）: コア体験の磨き込み
1. **一覧体験の改善**
   - キーワード検索（名前・ブランド・メモ）
   - 並び替え（登録日・使用回数）
   - 複合フィルタ（カテゴリ + 使用回数レンジ）
2. **登録体験の改善**
   - 画像 URL 入力に加えてファイルアップロード対応。
   - カテゴリを固定値 + 自由入力のハイブリッドに。
3. **詳細体験の改善**
   - 使用ログに「いつ・どんな場面で使ったか」を記録できるよう拡張。

**KPI**
- 1アイテム登録完了までの平均時間 30% 短縮。
- 一覧画面から詳細遷移率 20% 向上。

### Phase 2（2〜4 週間）: パーソナライズと継続率向上
1. **認証・ユーザー分離の本格化**
   - `DEFAULT_USER_ID` を廃止し、ログインユーザー基準に変更。
2. **レコメンド強化**
   - 最近使っていないアイテムの再提案。
   - カテゴリ偏りに対する「次に使う候補」表示。
3. **振り返りダッシュボード**
   - 週次/月次の使用頻度、カテゴリ比率、未使用期間を可視化。

**KPI**
- 週次継続利用率（WAU 維持）
- 「使用記録」投稿数の増加

## 4. 実装時の設計ガイド
- コントローラは薄く保ち、検索条件などの組み立ては Service 層で完結させる。
- DTO を入力/出力で明確に分け、将来の API 化に備える。
- 例外は `RuntimeException` 直投げを減らし、業務例外を定義してハンドリングする。
- 画像やログの拡張を見据え、DB スキーマは nullable 設計を明示する。

## 5. まず最初の 3 タスク（次の実装スプリント）
1. `application.yml` の認証情報を環境変数参照へ変更。
2. `DEFAULT_USER_ID` 依存の入口を洗い出して TODO 化。
3. `ItemService` のユニットテストを 3 ケース追加（create/update/increment）。

---
この計画をベースに、次は **Phase 0 のタスクを Issue 化**して、1タスクずつ小さく着手するのが最短です。
